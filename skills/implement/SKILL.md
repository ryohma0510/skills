---
name: implement
description: 機能追加・バグ修正など、実装作業に入るときに使う。'implement' や「実装して」と言われたときの入り口となるスキル。
---

# 実装

実装作業では `tdd` スキルを使ってテストファーストで進め、コメントを書く・見直すときは `code-comments` スキルを使う。実装が完了したら `subagent-review` スキルを使ってレビューする。
